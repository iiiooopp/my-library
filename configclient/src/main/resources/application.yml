server:
  port: 3302
management:
  endpoint:
    shutdown:
      enabled: false
  endpoints:
    web:
      exposure:
        include: "*"

data:
  env: "localhost-dev-edit"
  user:
    username: "username-dev"
    password: "password-dev"